# State

**Последнее обновление:** 2026-06-27

## Текущая волна

- **Задача:** Неделя 6 — Advanced CRUD + Projects
- **Статус:** ready to start (недели 1–5 ✓)

## Сделано (кратко)

- **Нед. 1–3:** health API, Prisma + Task CRUD, `/tasks` UI + `useTasks` + optimistic
- **Нед. 4:** Better Auth, RBAC, protected routes, login/register
- **Нед. 5:** unified API (`apiHandler`, Zod), `useApi` unwrap, `nitro.errorHandler`
- **Verify:** `pnpm verify && pnpm build` — OK (2026-06-27)

## Блокеры

- нет

## Next

1. Модель `Project` + relation с `Task`
2. Пагинация `?page=&limit=&q=` на tasks API
3. UI `/projects`, `/projects/[id]`
