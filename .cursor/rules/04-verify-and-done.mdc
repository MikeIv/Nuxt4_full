---
description: Verify, security (Nuxt/Nitro), code-review — критерии «готово»
alwaysApply: true
---

# Verify

## Перед «готово»

- [ ] brief / план
- [ ] проверки по классу (ниже)
- [ ] релевантные тесты
- [ ] UI — dev-сервер (`90-project-context`)
- [ ] Security (ниже)
- [ ] нет `console.log` / мёртвого кода

## Security (Nuxt/Nitro)

- [ ] секреты не в diff; `.env` не в git; `runtimeConfig`
- [ ] `server/api/*`: валидация body/query; ошибки без токенов/stack trace
- [ ] API-типы в `shared/types`
- [ ] `v-html` — санитизация
- [ ] auth/roles — по согласованию (`00`)
- [ ] HTTP: `useApi` / `serverApi`, не ручной `baseURL`

## Проверки по классу

| Класс  | Минимум                          |
| ------ | -------------------------------- |
| **S**  | ReadLints / lint                 |
| **M**  | lint; при API — `pnpm typecheck` |
| **L+** | lint + build; code-review        |

Не «готово», пока обязательные проверки не пройдены (если агент может запустить).

## Code-review

Триггеры: запрос, `/code-review`, Verify **L+**. Протокол — `.cursor/commands/code-review.md`.

Severity: **CRITICAL** секреты, XSS, ввод, auth · **HIGH** stack, raw `fetch`, `any` · **MEDIUM** a11y, edge · **LOW** стиль.

**L+ продукт:** spec · quality · consistency с репо.

**GitHub** (`gh pr`), не GitLab. Коммиты — только по просьбе; без push/`--no-verify` без разрешения.

## Не готово

CRITICAL/HIGH открыты; pipeline fail (L+); lint/build fail; открытые вопросы Discuss.
